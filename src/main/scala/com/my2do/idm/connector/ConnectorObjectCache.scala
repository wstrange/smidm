/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package com.my2do.idm.connector

import org.identityconnectors.framework.common.objects._


class ConnectorObjectCache {
  

  /*
  def foo = {
     val ops = new OperationOptionsBuilder()
    ops.setAttributesToGet(Name.NAME, Uid.NAME )

    accountTable.setLazyLoading(true)
    facade.search(ObjectClass.ACCOUNT,
                  null,
                  new ResultsHandler() {
                    def handle(obj: ConnectorObject):Boolean = {
                      //log.info("Found {}", obj)
                      accountTable.addItem(Array[Object](obj.getUid.getUidValue, obj.getName), obj)
                     
                      return true
                    }
                  },
                  ops.build)
  }

  */
}

