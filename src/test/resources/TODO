


Group support
    - Could have ResourceObject include an optional group object
                Assumes that resouruce supports a GROUP schema?
                Convenience used for handling special case of Group and Account (common use case)

Roles - merging / removing

Test Data generator


ICAttributes should implement Map interface

Complete functional test -

Implement concurrency - actors for recon?

Optimize ICF update - only send deltas

Auditing / Entitlements  - service layer


Is it easier to register "profiles" with Sync Manager. Cuts down on api complexity

Sync profile = sync rules + source? + correlation + schedule + auditing, error handling, etc.

Sync profile - can also have security attached?


